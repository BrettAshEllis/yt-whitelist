import React from "react";
import {
    BrowserRouter as
        Router,
    Routes,
    Route,
    Navigate,
    NavLink
} from "react-router-dom";
import { NavLink } from "react-router-dom";

function Menu() {
    return (
        <nav>
            <NavLink>

            </NavLink>
        </nav>
    )
}

export default Menu;