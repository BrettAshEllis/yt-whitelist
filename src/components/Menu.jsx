import React from "react";
import {
    BrowserRouter as
        Router,
    Routes,
    Route,
    Navigate,
} from "react-router-dom";
import { NavLink } from "react-router-dom";

export default Menu;