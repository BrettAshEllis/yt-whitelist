//! Display Channel thumbnail, title, and Description
//! Add button to add channel to whitelist

import react from "react"

function ChannelDisplay({ channel, addWhitelist, removeWhitelist, isWhitelist }) {
    return (
        <div>
            <h3>{whitelist.</h3>
        </div>
    )
}